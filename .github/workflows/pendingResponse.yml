  name: pending-response
  on:
    issue_comment:
      types: [created]

  jobs:
    issue_commented:
      if: ${{ !github.event.issue.pull_request  && contains(github.event.issue.labels.*.name, ':mailbox:') }}
      runs-on: ubuntu-latest
      steps:
        - uses: siegerts/pending-response@v1
          with:
            github-token: ${{ secrets.GITHUB_TOKEN }}
            member-associations: "OWNER, MEMBER"
            pending-response-label: "[X] :mailbox:"
            actionable-label: "[X] :basketball:"
